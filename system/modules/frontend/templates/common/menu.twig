{#
    Displays a menu that represents the current category tree

    To see available variables:
        - without enabling Twig debug mode: {{ d() }}
        - using native Twig debugger (Debug mode must be enabled): {{ dump() }}

    @package GPL Cart core
    @author Iurii Makukh <gplcart.software@gmail.com>
    @copyright Copyright (c) 2015, Iurii Makukh
    @license https://www.gnu.org/licenses/gpl.html GNU/GPLv3
    @link http://gplcart.com Support, modules, demo
#}
{% if items is not empty %}
    <ul class="nav navbar-nav menu-top">
      {% for item in items %}
          {% if item.depth <= depth %}
              <li class="depth-{{ item.depth }}{{ item.active ? ' active' : '' }}">
                {{ item.indentation|raw }}
                {% if item.active is empty %}
                    <a title="{{ item.title }}" href="{{ item.url }}">{{ item.title }}</a>
                {% else %}
                    <a class="disabled">{{ item.title }}</a>
                {% endif %}
              </li>
          {% endif %}
      {% endfor %}
    </ul>
{% endif %}
